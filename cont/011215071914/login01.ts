document.addEventListener("DOMContentLoaded", () => {
  // Example: Code to handle login functionality
  const loginButton = document.getElementById("loginButton");
  if (loginButton) {
    loginButton.addEventListener("click", () => {
      // Handle login
    });
  }
});
